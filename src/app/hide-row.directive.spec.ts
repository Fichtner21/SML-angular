import { HideRowDirective } from './hide-row.directive';

describe('HideRowDirective', () => {
  it('should create an instance', () => {
    const directive = new HideRowDirective();
    expect(directive).toBeTruthy();
  });
});
